pluginManagement {
    repositories {
        mavenLocal()
        maven {
            url = settings.ext.properties.get('MAVEN_REPO', 'https://nexus.statkart.no/repository/public/')
        }
    }
}

rootProject.name = 'matrikkel-wsclient'


gradle.rootProject { project -> //nested projects fallback to properties of root project
    project.ext.MAVEN_REPO = settings.ext.properties.get('MAVEN_REPO', 'https://nexus.statkart.no/repository/public/')
}

